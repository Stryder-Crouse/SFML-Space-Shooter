#pragma once
using namespace std;
#include <SFML/Graphics.hpp>
#include <SFML/Audio.hpp>
#include <iostream>
#include <string>
//#include <thread>
#include <chrono> 
#include <stdlib.h>
#include <time.h>
#include <random>
#include <windows.h>
#include <sstream>
#include <vector>
#include<math.h>
#include<fstream>
#include <sstream>

/*
if (moveD[0] == true) {
		mover2.x -= abs(cos(0) / 100 + trust);
		mover2.y -= abs(sin(0) / 10000 + trust);
	}
	if (moveD[1] == true) {
		mover2.x -= abs(cos(1) / 100 + trust);
		mover2.y -= abs(sin(1) / 10000 + trust);
	}
	if (moveD[2] == true) {
		mover2.x -= abs(cos(2) / 100 + trust);
		mover2.y -= abs(sin(2) / 10000 + trust);
	}
	if (moveD[3] == true) {
		mover2.x -= abs(cos(3) / 100 + trust);
		mover2.y -= abs(sin(3) / 10000 + trust);
	}
	if (moveD[4] == true) {
		mover2.x -= abs(cos(4) / 100 + trust);
		mover2.y -= abs(sin(4) / 10000 + trust);
	}
	if (moveD[5] == true) {
		mover2.x -= abs(cos(5) / 100 + trust);
		mover2.y -= abs(sin(5) / 10000 + trust);
	}
	if (moveD[6] == true) {
		mover2.x -= abs(cos(6) / 100 + trust);
		mover2.y -= abs(sin(6) / 10000 + trust);
	}
	if (moveD[7] == true) {
		mover2.x -= abs(cos(7) / 100 + trust);
		mover2.y -= abs(sin(7) / 10000 + trust);
	}
	if (moveD[8] == true) {
		mover2.x -= abs(cos(8) / 100 + trust);
		mover2.y -= abs(sin(8) / 10000 + trust);
	}
	if (moveD[9] == true) {
		mover2.x -= abs(cos(9) / 100 + trust);
		mover2.y -= abs(sin(9) / 10000 + trust);
	}

*/